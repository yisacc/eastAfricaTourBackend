{
  "routes": [
    {
      "method": "GET",
      "path": "/contact-uses",
      "handler": "contact-us.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contact-uses/count",
      "handler": "contact-us.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contact-uses/:id",
      "handler": "contact-us.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/contact-uses",
      "handler": "contact-us.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/contact-uses/:id",
      "handler": "contact-us.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contact-uses/:id",
      "handler": "contact-us.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
